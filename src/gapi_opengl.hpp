#pragma once

#include <GL/glew.h>

struct GApi {
};

struct GeometryBuffer {
    GLuint id;
};

struct GeometryVAO {
    GLuint id;
};

struct ShaderProgram {
};

struct Shader {
};

struct Texture2D {
};
